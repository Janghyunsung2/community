server:
  port: 8080


spring:
  profiles:
    active: dev
  data:
    redis:
      port: 6379
      password: ""
      host: 127.0.0.1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

S3:
  bucket:
    name: ontheit-image

aws:
  access: ${AWS_ACCESS_KEY_ID}
  secret: ${AWS_SECRET_ACCESS_KEY}

jwt:
  secret:
    key: mysecretkeymysecretkeymysecretkeymysecretkey

